# 当前进度

## 当前正在做什么
- 正在调整主题管理交互：主题类型用“普通主题 / 流程主题”页签切换，不再使用大卡片说明。

## 上次停在哪里
- 已改为流程主题只绑定一个 Flow；普通主题不再展示流程选择，也不会保存流程绑定。

## 近期关键决定和原因
- 普通主题用于自由问答和业务配置，只保留提示词、工具、实体等配置。
- 流程主题用于把用户问题路由到指定流程，只保留绑定流程入口。
- 主题类型区域去掉解释话术，只保留页签名称，避免配置页看起来像策划说明。
