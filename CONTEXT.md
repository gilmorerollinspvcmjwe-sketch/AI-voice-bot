当前正在做什么
- 按 PolyAI 的配置心智继续收敛前端原型，重点把变量页收成 State 管理，并把工具页改成简洁能力配置。

上次停在哪里
- 已把静默播音移到对话策略，这一轮完成了变量页和工具页的收口。

近期关键决定和原因
- 绑定能力不单独占一个大区块，改为贴着提示词的轻量按钮。
- 全局静默播音和全局超时播音都放在“对话策略”。
- 复用现有页面和组件，只做必要增改，不额外扩展新模块。
- 变量管理页以 State 为主视角，系统内置、自定义 State、默认值、写入规则分开显示。
- 工具配置页去掉解释性说明块，改成分类筛选加按钮式操作。
