# 当前进度

## 当前正在做什么
- 综合运营报表的 Flow 明细弹窗已改为单层内容容器。

## 上次停在哪里
- Step 明细弹窗去掉“大框套小框”，只保留一个白色弹窗内容框。
- Step / 边明细已补充搜索、列排序、固定表头、内部滚动、分页和每页条数切换。
- Step 搜索已在本地页面验证，可按 Step 名称或类型筛选。

## 近期关键决定和原因
- Flow 主表继续保留 Step 明细和边 / 分支两个入口，明细能力下沉到独立弹窗组件，避免主表文件继续膨胀。
- 弹窗内部直接展示表格和分页，不再包第二层卡片，减少视觉嵌套。
- 验证目标：静态检查、`npm run build` 和本地浏览器交互验证通过。
